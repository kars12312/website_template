{
  "$schema": "https://all-ai-network.org/schemas/integrations.schema.json",
  "version": "2.0.0",
  "description": "Canonical list of ALL Applied AI Network dashboard API integrations. Ground truth for the hub website template and for the Website tab in dashboard.all-ai-network.org, which generates the agent prompt from it. Every module is reachable WITHOUT an API key via the public bundle (see each module's publicSource) — that is the correct route for static sites and SPAs, which cannot hold a secret. The authenticated /api/v1/* endpoints are for sites that execute code on a server. Edit this file to add or change integrations.",
  "apiBase": "https://dashboard.all-ai-network.org",
  "envVar": "AAIN_API_KEY",
  "integrations": [
    {
      "id": "config",
      "title": "Hub-site configuration",
      "description": "Fetch your chapter's theme colors, logo URL, and section toggles from the dashboard. Drives the site's CSS variables, header logo, and which components render.",
      "iconKey": "settings",
      "accent": "#4f8fea",
      "endpoint": {
        "method": "GET",
        "path": "/api/v1/config",
        "example": "/api/v1/config",
        "queryParams": []
      },
      "templatePath": "src/main.ts",
      "snippets": {
        "javascript": "// Hub-site configuration — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { config } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/v1/config\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl -H \"Authorization: Bearer $AAIN_API_KEY\" \\\n  \"https://dashboard.all-ai-network.org/api/v1/config\"",
        "response": "{\n  \"config\": {\n    \"theme\": { \"primary\": \"#4f8fea\", \"accent\": \"#a855f7\" },\n    \"logo_url\": \"https://gdxifnnfirnzljtxnhch.supabase.co/storage/v1/object/public/chapter-logos/<chapter>/logo.png\",\n    \"sections\": {\n      \"hero\": true,\n      \"about\": true,\n      \"events\": true,\n      \"leaderboard\": true,\n      \"badges\": true,\n      \"merch\": true,\n      \"officers\": true,\n      \"learning_tree\": true,\n      \"playbooks\": true,\n      \"workshops\": true\n    },\n    \"updated_at\": \"2026-04-19T19:14:02.000Z\"\n  }\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nThe chapter's dashboard-side presentation settings: theme colors, logo URL, display name/tagline, and per-section on/off toggles.\n\nIMPORTANT — only apply these if this site was BUILT to be remotely themed. A site with its own established design should read the name, tagline and logo at most, and ignore theme colors and section toggles entirely. Overwriting a bespoke design with dashboard colors is the single most common way this integration goes wrong.\n\nIf you do apply the theme, set it as CSS custom properties on the root and let existing styles consume them — never duplicate the colors into individual rules. Missing section keys default to on.\n\nWhere it comes from: the public bundle's `config` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "config"
      }
    },
    {
      "id": "events",
      "title": "Upcoming events",
      "description": "Render your chapter's next events on your homepage. Auto-updates as you add events in the dashboard.",
      "iconKey": "calendar",
      "accent": "#4f8fea",
      "endpoint": {
        "method": "GET",
        "path": "/api/v1/events",
        "example": "/api/v1/events?upcoming=true&limit=5",
        "queryParams": [
          {
            "name": "upcoming",
            "type": "boolean",
            "description": "Return only events in the future"
          },
          {
            "name": "limit",
            "type": "integer",
            "description": "Max number of events to return (1-100)"
          }
        ]
      },
      "templatePath": "src/components/events-section.ts",
      "snippets": {
        "javascript": "// Upcoming events — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { events } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/v1/events?upcoming=true&limit=5\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl -H \"Authorization: Bearer $AAIN_API_KEY\" \\\n  \"https://dashboard.all-ai-network.org/api/v1/events?upcoming=true&limit=5\"",
        "response": "{\n  \"events\": [\n    {\n      \"id\": \"evt_abc123\",\n      \"title\": \"Intro to Applied AI\",\n      \"description\": \"Kick-off session for the semester.\",\n      \"type\": \"workshop\",\n      \"date\": \"2026-09-12T23:30:00.000Z\",\n      \"points_attend\": 10,\n      \"points_win\": 0,\n      \"checkin_count\": 42\n    }\n  ]\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nThe chapter's events: title, description, type, start/end, timezone, location or virtual URL, cover image, attendance points, and phases for multi-part events. Co-hosted events include the host chapter so you can credit them.\n\nScope matters: the bundle returns ONLY UPCOMING events by default. A chapter whose events are all in the past gets an empty array — check the `events_scope` field before concluding it has none. Add `?events=all` for a full archive (newest first), `?events=past`, or `&events_limit=N` to raise the cap. A site with an events history page wants `all`; a homepage strip wants the default.\n\nShow at least title, a human-formatted date in the event's own timezone, and location. Link to the event's own page if the site has one.\n\nWhere it comes from: the public bundle's `events` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "events"
      }
    },
    {
      "id": "leaderboard",
      "title": "Leaderboard",
      "description": "Show the top members by XP. Great for a homepage widget or a dedicated members page.",
      "iconKey": "leaderboard",
      "accent": "#f59e0b",
      "endpoint": {
        "method": "GET",
        "path": "/api/v1/leaderboard",
        "example": "/api/v1/leaderboard?limit=10",
        "queryParams": [
          {
            "name": "limit",
            "type": "integer",
            "description": "Max number of members to return (1-100)"
          }
        ]
      },
      "templatePath": "src/components/leaderboard-widget.ts",
      "snippets": {
        "javascript": "// Leaderboard — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { leaderboard } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/v1/leaderboard?limit=10\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl -H \"Authorization: Bearer $AAIN_API_KEY\" \\\n  \"https://dashboard.all-ai-network.org/api/v1/leaderboard?limit=10\"",
        "response": "{\n  \"leaderboard\": [\n    { \"name\": \"Jane Doe\",   \"points\": 1250, \"events_attended\": 18, \"rank\": 1 },\n    { \"name\": \"John Smith\", \"points\": 1100, \"events_attended\": 16, \"rank\": 2 },\n    { \"name\": \"Aisha Khan\", \"points\":  980, \"events_attended\": 14, \"rank\": 3 }\n  ]\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nTop members by points, each with rank, name, events attended, and the badges they've earned (name + icon URL).\n\nThis is real people's names on a public page — render it plainly and don't add anything the dashboard didn't send. Rank, name, and points are the essentials; badges are a nice secondary row if the site has room.\n\nWhere it comes from: the public bundle's `leaderboard` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "leaderboard"
      }
    },
    {
      "id": "chapter",
      "title": "Chapter stats",
      "description": "Pull your chapter's name, university, member count, and event count — perfect for a hero banner or footer.",
      "iconKey": "building",
      "accent": "#a855f7",
      "endpoint": {
        "method": "GET",
        "path": "/api/v1/chapter",
        "example": "/api/v1/chapter",
        "queryParams": []
      },
      "templatePath": "src/components/hero.ts",
      "snippets": {
        "javascript": "// Chapter stats — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { chapter } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/v1/chapter\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl -H \"Authorization: Bearer $AAIN_API_KEY\" \\\n  \"https://dashboard.all-ai-network.org/api/v1/chapter\"",
        "response": "{\n  \"chapter\": {\n    \"name\": \"MSOE AI Club\",\n    \"university\": \"Milwaukee School of Engineering\",\n    \"slug\": \"msoe\",\n    \"member_count\": 500,\n    \"event_count\": 142\n  }\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nChapter identity and live counts: name, university, member count, event count.\n\nUse these to replace any hardcoded figures already in the site's copy (\"500+ members\" and the like) so they stop drifting. Keep the existing presentation — if the site already has a stats strip, swap its numbers rather than adding a second one.\n\nWhere it comes from: the public bundle's `chapter` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "chapter"
      }
    },
    {
      "id": "badges",
      "title": "Chapter badges",
      "description": "Render the full catalog of recognition badges your chapter awards — with their name, description, icon, and how many members have earned each one.",
      "iconKey": "award",
      "accent": "#10b981",
      "endpoint": {
        "method": "GET",
        "path": "/api/v1/badges",
        "example": "/api/v1/badges?limit=100",
        "queryParams": [
          {
            "name": "limit",
            "type": "integer",
            "description": "Max badges to return (1-500, default 100)"
          }
        ]
      },
      "templatePath": "src/components/badges-wall.ts",
      "snippets": {
        "javascript": "// Chapter badges — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { badges } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/v1/badges?limit=100\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl -H \"Authorization: Bearer $AAIN_API_KEY\" \\\n  \"https://dashboard.all-ai-network.org/api/v1/badges\"",
        "response": "{\n  \"badges\": [\n    {\n      \"id\": \"bdg_nvidia_dli\",\n      \"name\": \"NVIDIA DLI\",\n      \"description\": \"Completed an NVIDIA Deep Learning Institute certification.\",\n      \"icon\": \"https://gdxifnnfirnzljtxnhch.supabase.co/storage/v1/object/public/badge-icons/<chapter>/nvidia-dli.png\",\n      \"award_count\": 23\n    },\n    {\n      \"id\": \"bdg_rosie_finalist\",\n      \"name\": \"ROSIE Competition Finalist\",\n      \"description\": \"Top 8 finish in the annual MAIC ROSIE hackathon.\",\n      \"icon\": \"award\",\n      \"award_count\": 16\n    }\n  ]\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nEvery recognition badge the chapter awards: name, description, icon, and how many members have earned it.\n\nThe `icon` field has two forms. An https URL is a custom uploaded image — render it as an image. Anything else is a short built-in key (\"trophy\", \"award\", \"star\"…) — map it to whatever icon set the site already uses, falling back to a generic award icon for keys you don't recognize.\n\nSort by award count descending; the most-earned badges are what visitors care about.\n\nWhere it comes from: the public bundle's `badges` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "badges"
      }
    },
    {
      "id": "merch",
      "title": "Merch catalog",
      "description": "Display the rewards your chapter offers in exchange for earned points — T-shirts, stickers, workshop vouchers — so members know what they're working toward.",
      "iconKey": "package",
      "accent": "#ec4899",
      "endpoint": {
        "method": "GET",
        "path": "/api/v1/merch",
        "example": "/api/v1/merch?limit=50",
        "queryParams": [
          {
            "name": "include",
            "type": "string",
            "description": "Pass `all` to include hidden + out-of-stock items. Default returns only active items."
          },
          {
            "name": "limit",
            "type": "integer",
            "description": "Max items to return (1-500, default 100)"
          }
        ]
      },
      "templatePath": "src/components/merch-catalog.ts",
      "snippets": {
        "javascript": "// Merch catalog — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { merch } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/v1/merch?limit=50\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl -H \"Authorization: Bearer $AAIN_API_KEY\" \\\n  \"https://dashboard.all-ai-network.org/api/v1/merch\"",
        "response": "{\n  \"merch\": [\n    {\n      \"id\": \"mrch_tshirt\",\n      \"name\": \"MAIC T-Shirt\",\n      \"description\": \"Classic black tee with the MAIC logo.\",\n      \"cost_points\": 75,\n      \"image_url\": \"https://gdxifnnfirnzljtxnhch.supabase.co/storage/v1/object/public/merch-images/<chapter>/tshirt.jpg\",\n      \"stock\": 12,\n      \"is_active\": true,\n      \"created_at\": \"2026-04-19T14:02:31.000Z\"\n    },\n    {\n      \"id\": \"mrch_stickers\",\n      \"name\": \"Sticker pack\",\n      \"description\": \"Three vinyl stickers — MAIC logo, ROSIE, NVIDIA-Olympus.\",\n      \"cost_points\": 15,\n      \"image_url\": null,\n      \"stock\": null,\n      \"is_active\": true,\n      \"created_at\": \"2026-04-19T14:03:11.000Z\"\n    }\n  ]\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nThe rewards members can redeem with points: name, description, point cost, image, and stock.\n\nTwo fields have meaningful null/zero cases. `stock`: null means unlimited (hide the stock line), 0 means out of stock (still list it, visibly muted — not hidden, or it reads as a broken listing), any positive number means that many left. `image_url`: null means render a placeholder in the site's own style, not a broken image.\n\nThis is a display-only catalog — redemption happens in person, so don't build a cart or checkout. One line explaining how to redeem is worth including.\n\nWhere it comes from: the public bundle's `merch` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "merch"
      }
    },
    {
      "id": "socials",
      "title": "Club social feed",
      "description": "Latest public posts from the club's linked LinkedIn and Instagram accounts, synced on demand from the dashboard. Renders as a card grid; hides itself until the eboard has synced a feed.",
      "iconKey": "share",
      "accent": "#06b6d4",
      "endpoint": {
        "method": "GET",
        "path": "/api/public/chapter/{slug}/bundle",
        "example": "/api/public/chapter/{slug}/bundle",
        "queryParams": []
      },
      "templatePath": "src/main.ts",
      "snippets": {
        "javascript": "// Club social feed — from the public bundle. No API key required,\n// which is what a static site or SPA must use.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { social_feeds } = await res.json();\n\n// Server-rendered sites may instead call the per-module endpoint\n// with a key that never reaches the browser:\n//   GET https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\n//   Authorization: Bearer <AAIN_API_KEY>\n",
        "curl": "curl \"https://dashboard.all-ai-network.org/api/public/chapter/YOUR-CHAPTER-SLUG/bundle\" | jq .social_feeds",
        "response": "{\n  \"social_feeds\": {\n    \"linkedin\": {\n      \"source_url\": \"https://www.linkedin.com/company/msoe-ai-club\",\n      \"synced_at\": \"2026-07-16T14:03:22.000Z\",\n      \"posts\": [\n        {\n          \"id\": \"7351234567890\",\n          \"url\": \"https://www.linkedin.com/posts/...\",\n          \"posted_at\": \"2026-07-12T18:30:00.000Z\",\n          \"content\": \"Our Spring Innovation Lab teams just demoed...\",\n          \"image_url\": \"https://media.licdn.com/dms/image/...\",\n          \"likes\": 42,\n          \"comments\": 7\n        }\n      ]\n    },\n    \"instagram\": {\n      \"source_url\": \"https://www.instagram.com/msoe.aiclub/\",\n      \"synced_at\": \"2026-07-16T14:05:10.000Z\",\n      \"posts\": [\n        {\n          \"id\": \"3391234567890\",\n          \"url\": \"https://www.instagram.com/p/ABC123/\",\n          \"posted_at\": \"2026-07-10T21:00:00.000Z\",\n          \"content\": \"Workshop night recap!\",\n          \"image_url\": \"https://scontent.cdninstagram.com/...\",\n          \"likes\": 88,\n          \"comments\": 12\n        }\n      ]\n    }\n  }\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nRecent public posts from the chapter's linked social accounts, keyed by platform. Each platform carries its source URL, last sync time, and posts with caption, image, link, and engagement counts.\n\nMerge across platforms, sort newest first, and cap at a handful. Label which platform each came from and link to the original in a new tab.\n\nPlatforms the eboard hasn't synced are simply absent, and an unsynced chapter has none at all — so this section must hide itself cleanly rather than render an empty grid.\n\nWhere it comes from: the public bundle's `social_feeds` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "social_feeds"
      }
    },
    {
      "id": "projects",
      "title": "Projects & research",
      "description": "The chapter's project and research history — each with its team, papers, slides, and recordings.",
      "iconKey": "users",
      "accent": "#8b5cf6",
      "endpoint": {
        "method": "GET",
        "path": "/api/public/chapter/{slug}/bundle",
        "example": "/api/public/chapter/{slug}/bundle"
      },
      "publicSource": {
        "path": "/api/public/chapter/{slug}/bundle",
        "field": "projects"
      },
      "snippets": {
        "javascript": "// Projects, with their teams and attached papers. No API key needed.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"\n);\nconst { projects } = await res.json();\n\nfor (const p of projects) {\n  const team = p.members.map((m) => m.name).join(\", \");\n  const paper = p.files.find((f) => f.kind === \"paper\");\n  console.log(p.year, p.title, \"—\", team, paper?.url ?? \"\");\n}",
        "curl": "curl \"https://dashboard.all-ai-network.org/api/public/chapter/{slug}/bundle\"",
        "response": "{\n  \"projects\": [\n    {\n      \"id\": \"\\u2026\",\n      \"title\": \"NourishNet\",\n      \"description\": \"Forecasting food commodity price spikes\\u2026\",\n      \"image_url\": \"https://\\u2026/team-logo.png\",\n      \"link_url\": null,\n      \"year\": \"2023-2024\",\n      \"members\": [\n        {\n          \"name\": \"Sydney Balboni\",\n          \"role\": \"author\"\n        },\n        {\n          \"name\": \"John Cisler\",\n          \"role\": \"author\"\n        }\n      ],\n      \"files\": [\n        {\n          \"id\": \"\\u2026\",\n          \"kind\": \"paper\",\n          \"title\": \"NourishNet \\u2014 paper\",\n          \"url\": \"https://arxiv.org/pdf/2407.00698\"\n        }\n      ]\n    }\n  ]\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nThe chapter's project and research history. Each project has a title, description, cover image, school year, the team that built it (`members`, in byline order), and `files` — papers, slide decks, and recordings, each tagged with a `kind`.\n\nThe team and the papers are the point. A project list without names is a wall of titles; with names it's a record of who did what, and alumni will look themselves up. Show the byline and link any paper prominently.\n\nGroup by `year` if the chapter has several years of history — that's usually the most legible ordering. `files` may be empty; `members` may be a single name.\n\nWhere it comes from: the public bundle's `projects` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      }
    },
    {
      "id": "learning-tree",
      "title": "Learning tree",
      "description": "The network's shared AI curriculum graph merged with this chapter's own nodes.",
      "iconKey": "tree",
      "accent": "#14b8a6",
      "endpoint": {
        "method": "GET",
        "path": "/api/public/learning-tree/{slug}",
        "example": "/api/public/learning-tree/{slug}"
      },
      "publicSource": {
        "path": "/api/public/learning-tree/{slug}",
        "note": "Its own endpoint — not part of the bundle."
      },
      "snippets": {
        "javascript": "// Merged curriculum graph: shared base tree + this chapter's nodes.\nconst res = await fetch(\n  \"https://dashboard.all-ai-network.org/api/public/learning-tree/{slug}\"\n);\nconst { nodes, edges } = await res.json();\n\n// nodes carry pos_x/pos_y when an officer has arranged them —\n// honour those and only auto-layout the ones without.\nconst pinned = nodes.filter((n) => n.pos_x != null && n.pos_y != null);\nconsole.log(`${nodes.length} nodes, ${pinned.length} hand-placed`);",
        "curl": "curl \"https://dashboard.all-ai-network.org/api/public/learning-tree/{slug}\"",
        "response": "{\n  \"chapter\": {\n    \"slug\": \"\\u2026\",\n    \"name\": \"\\u2026\"\n  },\n  \"nodes\": [\n    {\n      \"id\": \"foundations/what-is-ai\",\n      \"title\": \"What Is AI? The Real Story\",\n      \"summary\": \"\\u2026\",\n      \"body\": null,\n      \"parent_ref\": null,\n      \"prereqs\": [],\n      \"tags\": [\n        \"foundations\"\n      ],\n      \"thumbnail\": \"https://\\u2026\",\n      \"difficulty\": \"beginner\",\n      \"pos_x\": -1240,\n      \"pos_y\": 0,\n      \"source\": \"base\"\n    }\n  ],\n  \"edges\": [\n    {\n      \"from\": \"\\u2026\",\n      \"to\": \"\\u2026\"\n    }\n  ]\n}",
        "agentPrompt": "Render this from the chapter's dashboard so the eboard controls it without touching this repo.\n\nThe curriculum graph: the network's shared base tree merged with the chapter's own nodes, as `nodes` + `edges`. Each node carries a title, summary, optional markdown body, tags, thumbnail, difficulty, prerequisites, and a `source` marking it as shared or chapter-authored.\n\nRespect `pos_x`/`pos_y` where present — an officer arranged those by hand in the dashboard, and overriding them with an auto-layout throws away deliberate work. Only compute positions for nodes that have none.\n\n`prereqs` can name more than one parent: this is a graph, not a strict tree, so a renderer that assumes one parent per node will silently drop edges.\n\nIf the site already renders a tree from its own data file, this endpoint replaces that file as the source of truth — the chapter edits nodes in the dashboard from now on.\n\nWhere it comes from: the public bundle's `nodes` field — GET /api/public/chapter/{slug}/bundle. No API key, CORS-open, safe to call from the browser. If this site runs code on a server at request or build time you may use the authenticated per-module endpoint above instead; if it does not (static site or SPA), use the bundle and do not introduce a key.\n\nHow to fit it in:\n- If the site already has a section showing this, REWIRE it — change its data source and leave its markup and styling alone. Only build something new if nothing equivalent exists.\n- Match the site's existing components, spacing, and type scale. Inspect a sibling section before writing any CSS.\n- Escape remote text; only linkify http(s) URLs.\n- Empty response → omit the section entirely rather than render an empty shell. Fetch failure → leave the page exactly as it was.\n"
      }
    }
  ]
}
